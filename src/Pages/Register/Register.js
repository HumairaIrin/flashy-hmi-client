import React from 'react';
import useTitle from '../../hooks/useTitle';
import './Register.css';

const Register = () => {
    useTitle('Register');
    return (
        <div>
            <h2>register page</h2>
        </div>
    );
};

export default Register;