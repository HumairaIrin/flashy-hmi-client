import React from 'react';
import useTitle from '../../hooks/useTitle';
import './Login.css';

const Login = () => {
    useTitle('Login');
    return (
        <div>
            <h4>login page</h4>
        </div>
    );
};

export default Login;